import { FormattedResult, IacFileScanResult, IaCTestFlags } from './types';
import { CustomError } from '../../../../lib/errors';
export declare function formatScanResults(scanResults: IacFileScanResult[], options: IaCTestFlags): FormattedResult[];
export declare class FailedToFormatResults extends CustomError {
    constructor(message?: string);
}
